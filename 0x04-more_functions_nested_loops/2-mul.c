#include "main.h"

/**
 * mul - multiplies two integers
 * @x: number to multiply
 * @y: second number to multiply 
 * Return: the result of x mult by y
 */

int mul(int x, int y)
{
	return (x * y);
}
