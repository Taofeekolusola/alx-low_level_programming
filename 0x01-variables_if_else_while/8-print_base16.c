#include <stdio.h>
#include <stdlib.h>

/**
 * main - all the numbers of base 16
 * Return: Always 0 (Success)
 */

int main(void)
{
	int num;

	for (num = 0; num <= 16; num++)
	putchar(num);
	putchar('\n');

	return (0);
}
